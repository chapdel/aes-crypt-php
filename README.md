# AES Crypt PHP
Crypt and decrypt on php for javascript client with AES CBC Method

## Installation

This package can be installed via Composer by running following command:

```
    composer require chapdel/aescrypt
```
